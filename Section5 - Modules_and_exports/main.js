//import logger from './logger';
//node js crea una cache de nuestro codigo cada vez que utilizamos un require. | Singleton

//const logger = require('./logger') 
const {greet, number} = require("./logger"); //ctrl + space = autocomplete
greet("Rodri")
console.log(number);